# Optional Map Assets

Place optional geographic overlay files here.

Supported optional file:

- `hk_districts.geojson` (Hong Kong district boundaries)

If present, the app can use it for richer district-level map overlays.

